# Agent metadata
AGENT_NAME=inference-chip-index
AGENT_VERSION=1.0.0
AGENT_DESCRIPTION=Exact-slice, source-linked MLPerf Inference v6.0 comparisons of inference accelerators

# Local server
PORT=3000

# Optional x402 seller configuration for Base Sepolia. Leave the whole group
# unset for a free runtime. Priced entrypoints fail closed until this group is
# complete — they never become free.
# PAYMENTS_FACILITATOR_URL=https://x402.org/facilitator
# PAYMENTS_NETWORK=eip155:84532
# PAYMENTS_DESTINATION=static
# PAYMENTS_RECEIVABLE_ADDRESS=0xYOUR_EVM_ADDRESS
